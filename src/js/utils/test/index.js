/* eslint-env mocha */
import 'should';// eslint-disable-line

describe('Test', () => {
  describe('Assertions', () => {
    it('assertions should work', () => {
      (1).should.be.exactly(1)
    })
  })
})
